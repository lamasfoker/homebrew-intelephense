name: Update intelephense

on:
  schedule:
    - cron: "0 3 * * 1" # weekly, Monday 03:00 UTC
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write

jobs:
  bump:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Bump formula (intelephense version + vendored Node.js LTS)
        id: bump
        run: script/bump.sh

      - name: Show diff
        run: git diff --stat

      - name: Build and test the bumped formula
        if: steps.bump.outputs.changed == '1'
        run: |
          TAP_DIR="$(brew --repository)/Library/Taps/lamasfoker"
          mkdir -p "$TAP_DIR"
          ln -sfn "$GITHUB_WORKSPACE" "$TAP_DIR/homebrew-intelephense"
          brew install --formula --build-from-source lamasfoker/intelephense/intelephense
          brew test lamasfoker/intelephense/intelephense

      - name: Open PR
        id: pr
        if: steps.bump.outputs.changed == '1'
        uses: peter-evans/create-pull-request@v6
        with:
          commit-message: "intelephense: automated version bump"
          branch: automated/bump-intelephense
          delete-branch: true
          title: "intelephense: automated version bump"
          body: |
            Automated bump by `.github/workflows/update.yml`.
            `brew install --build-from-source` + `brew test` passed on macos-14 before this PR was opened.

      - name: Enable auto-merge
        if: steps.bump.outputs.changed == '1' && steps.pr.outputs.pull-request-number
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr merge --squash --auto "${{ steps.pr.outputs.pull-request-number }}"
